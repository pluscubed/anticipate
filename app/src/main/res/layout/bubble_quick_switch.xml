<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/bubble_size_padding"
    android:layout_height="@dimen/bubble_size_padding"
    android:orientation="vertical">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/bubble_shadow" />

    <FrameLayout
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center"
        android:background="@drawable/bubble"
        android:elevation="4dp">

        <ImageView
            android:id="@id/bubble_quick_switch_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:padding="16dp" />

        <com.pluscubed.anticipate.widget.ProgressWheel
            android:id="@id/bubble_quick_switch_progress"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:matProg_barWidth="2dp"
            app:matProg_fillRadius="true"
            app:matProg_progressIndeterminate="true" />


    </FrameLayout>

</FrameLayout>